<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dkd.manage.mapper.TestDataInsertMapper">
    
    <!-- 插入测试数据 -->
    <insert id="insertTestData">
        INSERT INTO tb_real_time_data_verification (
            id,
            user_mobile,
            event_key,
            client_time,
            event_type,
            event_time,
            session_id,
            platform,
            browser,
            client_version,
            domain,
            ip,
            user_agent,
            sdk_version,
            data_source_id,
            attributes
        ) VALUES (
            #{id},
            #{userMobile},
            'clk',
            #{eventTime},
            'CUSTOM_EVENT',
            #{eventTime},
            CONCAT('session_test_', #{id}),
            'web',
            'Chrome Mobile|Chrome Mobile 141',
            '1.0.0',
            'dev.coc.10086.cn',
            '39.183.168.62',
            'Mozilla/5.0 (Linux; Android 6.0; Nexus 5 Build/MRA58N)',
            '3.8.6',
            'x9nzkqwdpm680ta3',
            CONCAT('{"$path":"/test/page","$title":"测试页面_', #{id}, '","WT_mc_ev":"test_event"}')
        )
    </insert>
    
    <!-- 查询最大ID -->
    <select id="selectMaxId" resultType="java.lang.Long">
        SELECT COALESCE(MAX(id), 0) FROM tb_real_time_data_verification
    </select>

    <!-- 插入测试数据（内容可变长版，各列内容由调用方按档位算好传入） -->
    <insert id="insertTestDataRich">
        INSERT INTO tb_real_time_data_verification (
            id,
            user_mobile,
            event_key,
            client_time,
            event_type,
            event_time,
            session_id,
            platform,
            browser,
            client_version,
            domain,
            ip,
            user_agent,
            sdk_version,
            data_source_id,
            attributes
        ) VALUES (
            #{id},
            #{userMobile},
            #{eventKey},
            #{eventTime},
            'CUSTOM_EVENT',
            #{eventTime},
            CONCAT('session_test_', #{id}),
            #{platform},
            #{browser},
            '1.0.0',
            #{domain},
            #{ip},
            #{userAgent},
            #{sdkVersion},
            #{dataSourceId},
            #{attributes}
        )
    </insert>

</mapper>
