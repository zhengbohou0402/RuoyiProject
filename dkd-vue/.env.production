# 页面标题
VITE_APP_TITLE = 智柜管理系统

# 生产环境配置
VITE_APP_ENV = 'production'

# 智柜管理系统/生产环境
VITE_APP_BASE_API = '/prod-api'

# 是否在打包时开启静态资源压缩，支持 gzip 和 brotli。
# 当前关闭压缩，避免 Windows 下 dist 目录已有 .gz 文件被占用时构建失败。
VITE_BUILD_COMPRESS =
